# Copy to .env and adjust as needed, then run with:
#   make run

# Port the server listens on
PORT=8080

# Directory containing your movie subfolders (default: movies)
MOVIES_DIR=/movies

# Directory containing your series subfolders (default: series)
SERIES_DIR=/series
